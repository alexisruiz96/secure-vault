{
  "name": "secure-vault-client",
  "version": "1.0.0",
  "description": "Manage cloud storage using secure passwords and encrypted data.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "prepublishOnly": "npm run build"
  },
  "keywords": [],
  "author": "aruizalcala",
  "license": "ISC",
  "devDependencies": {
    "axios": "^0.27.2",
    "tsc": "^2.0.4",
    "typescript": "^4.8.3"
  }
}
